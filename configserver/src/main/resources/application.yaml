server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AnilPatamber/workspace
          search-paths:          
          - remoteconfig*
          - remoteconfig/config*